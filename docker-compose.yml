version: '3.9' # optional since v1.27.0
services:
  iris3api:
    build: .
    ports:
      - '8001:8000'
    volumes:
      - .:/code
      - logvolume01:/var/log
    links:
      - redis
  redis:
    image: redis
    ports:
      - '6379:6379'
volumes:
  logvolume01: {}
